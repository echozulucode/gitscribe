[package]
name = "context_core"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0.100"
reqwest = { version = "0.12", features = ["blocking", "json"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
